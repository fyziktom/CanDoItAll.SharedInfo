interface:
  display_name: "Canonical Model Review"
  short_description: "Review canonical model integrity and boundaries"
  default_prompt: "Use $canonical-model-review to review canonical truth, boundaries, and stabilization priorities."
policy:
  allow_implicit_invocation: false
